spring:
  data:
    mongodb:
      uri: ${MONGO_URI}

logging:
  level:
    root: WARN
    com.VAMA.VAMA_BACKEND: INFO
    org.springframework.security: WARN
    org.springframework.data.mongodb: WARN
